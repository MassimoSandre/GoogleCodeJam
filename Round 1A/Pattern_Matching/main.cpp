#include <bits/stdc++.h>

using namespace std;

int main() {
    int t,n;
    cin >> t;
    for(int i = 0; i < t;i++) {
        cin >> n;
        vector<string> v(n);
        string ris;
        vector<int> len(n);
        for(int j = 0; j < n;j++) {
            cin >> v[j];
            len[j] = v[j].length();
        }
        for(int j =1; j < n;j++) {

        }
    }
    return 0;
}
